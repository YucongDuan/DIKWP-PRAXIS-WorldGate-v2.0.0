# GitHub publication record

Creator: Yucong Duan (段玉聪).

## Import scope

This repository publishes the supplied 2.0.0 project with directly browsable source, existing documentation, tests and release artifacts. Runtime source and test contents are preserved byte for byte. Public packaging adds author and citation metadata, download navigation and this publication record. Root README publication status is updated; the original delivery is preserved unchanged.

Original archive: [`praxis_v2.zip`](release/praxis_v2.zip).

SHA-256: `5d8fe3d0a3631268071006c4901d67fc5070081a6d0ff9fd4f900d32fe4de453`.

Original manifests and validation reports describe the supplied release before GitHub packaging. Use the original archive to reproduce those exact historical file hashes; `publication/FILES.sha256` describes the packaged repository files.

## Verification

221 existing tests passed locally on Python 3.12.14 with zero failures and zero errors. See [`prepublish-tests.json`](publication/prepublish-tests.json). This confirms these supplied tests in this environment; it is not independent certification or a production deployment.

## Duplicate input

The uploaded `praxis_v2 - 副本.zip` and `praxis_v2.zip` have the same SHA-256 and identical bytes. Both inputs are represented by this one project.
